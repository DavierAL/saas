# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: login.spec.ts >> Login Page >> should show validation error for empty password
- Location: e2e\login.spec.ts:25:3

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('text=password, text=Password, text=contraseña')
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for locator('text=password, text=Password, text=contraseña')

```

# Page snapshot

```yaml
- generic [ref=e4]:
  - generic [ref=e5]:
    - generic [ref=e6]: P
    - generic [ref=e7]: SaaS POS
  - heading "Iniciar sesión" [level=1] [ref=e8]
  - paragraph [ref=e9]: Accede a tu punto de venta
  - generic [ref=e10]:
    - generic [ref=e11]:
      - generic [ref=e12]: Email
      - textbox "cajero@negocio.com" [ref=e13]: test@example.com
    - generic [ref=e14]:
      - generic [ref=e15]: Contraseña
      - textbox "••••••••" [active] [ref=e16]
    - button "Ingresar" [ref=e17] [cursor=pointer]
  - generic [ref=e18]:
    - generic [ref=e19]: ¿No tienes cuenta?
    - button "Crear cuenta" [ref=e20] [cursor=pointer]
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test.describe('Login Page', () => {
  4  |   test.beforeEach(async ({ page }) => {
  5  |     await page.goto('/login');
  6  |   });
  7  | 
  8  |   test('should display login form', async ({ page }) => {
  9  |     await expect(page.locator('h1, h2, [data-testid="login-title"]')).toBeVisible();
  10 |     await expect(page.locator('input[type="email"], input[name="email"]')).toBeVisible();
  11 |     await expect(page.locator('input[type="password"], input[name="password"]')).toBeVisible();
  12 |     await expect(page.locator('button[type="submit"]')).toBeVisible();
  13 |   });
  14 | 
  15 |   test('should have link to register page', async ({ page }) => {
  16 |     const registerLink = page.locator('a[href="/register"]');
  17 |     await expect(registerLink).toBeVisible();
  18 |   });
  19 | 
  20 |   test('should show validation error for empty email', async ({ page }) => {
  21 |     await page.locator('button[type="submit"]').click();
  22 |     await expect(page.locator('text=email, text=Email, text=correo')).toBeVisible();
  23 |   });
  24 | 
  25 |   test('should show validation error for empty password', async ({ page }) => {
  26 |     await page.locator('input[type="email"], input[name="email"]').fill('test@example.com');
  27 |     await page.locator('button[type="submit"]').click();
> 28 |     await expect(page.locator('text=password, text=Password, text=contraseña')).toBeVisible();
     |                                                                                 ^ Error: expect(locator).toBeVisible() failed
  29 |   });
  30 | });
  31 | 
  32 | test.describe('Register Page', () => {
  33 |   test.beforeEach(async ({ page }) => {
  34 |     await page.goto('/register');
  35 |   });
  36 | 
  37 |   test('should display register form', async ({ page }) => {
  38 |     await expect(page.locator('input[type="email"], input[name="email"]')).toBeVisible();
  39 |     await expect(page.locator('input[type="password"], input[name="password"]')).toBeVisible();
  40 |     await expect(page.locator('button[type="submit"]')).toBeVisible();
  41 |   });
  42 | 
  43 |   test('should have link back to login', async ({ page }) => {
  44 |     const loginLink = page.locator('a[href="/login"]');
  45 |     await expect(loginLink).toBeVisible();
  46 |   });
  47 | });
  48 | 
  49 | test.describe('Protected Routes', () => {
  50 |   test('should redirect to /login when accessing /orders without auth', async ({ page }) => {
  51 |     await page.goto('/orders');
  52 |     await expect(page).toHaveURL(/login/);
  53 |   });
  54 | 
  55 |   test('should redirect to /login when accessing /inventory without auth', async ({ page }) => {
  56 |     await page.goto('/inventory');
  57 |     await expect(page).toHaveURL(/login/);
  58 |   });
  59 | 
  60 |   test('should redirect to /login when accessing /analytics without auth', async ({ page }) => {
  61 |     await page.goto('/analytics');
  62 |     await expect(page).toHaveURL(/login/);
  63 |   });
  64 | 
  65 |   test('should redirect to /login when accessing root without auth', async ({ page }) => {
  66 |     await page.goto('/');
  67 |     await expect(page).toHaveURL(/login/);
  68 |   });
  69 | });
  70 | 
  71 | test.describe('Navigation', () => {
  72 |   test('should allow access to login page without auth', async ({ page }) => {
  73 |     await page.goto('/login');
  74 |     await expect(page).toHaveURL(/login/);
  75 |   });
  76 | 
  77 |   test('should allow access to register page without auth', async ({ page }) => {
  78 |     await page.goto('/register');
  79 |     await expect(page).toHaveURL(/register/);
  80 |   });
  81 | });
```